# ==============================================
# TypoScript object for generating nonces
# ==============================================

lib.cspNonce = USER_INT
lib.cspNonce {
    userFunc = LimeSoda\LsSecurityHeaders\UserFunctions\Csp->generateNonce

    length = TEXT
    length.field = length
    policy = TEXT
    policy.field = policy
    asAttribute = TEXT
    asAttribute.field = asAttribute
}
